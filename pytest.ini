[pytest]
pythonpath = .
testpaths = tests
norecursedirs = airflow logs venv .git __pycache__
markers =
    unit: fast tests, no external dependency
    integration: requires Docker network (MinIO, Postgres)